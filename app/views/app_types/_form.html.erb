<%= form_for(@app_type) do |f| %>
  <% if @app_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app_type.errors.count, "error") %> prohibited this app_type from being saved:</h2>

      <ul>
      <% @app_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :product %><br>
    <%= f.collection_select(:product_id, Product.all, :id, :name, include_blank: false)%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
