<%= form_for(@gear_profile) do |f| %>
  <% if @gear_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gear_profile.errors.count, "error") %> prohibited this gear_profile from being saved:</h2>

      <ul>
      <% @gear_profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
